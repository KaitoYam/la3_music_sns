<!DOCTYPE html>
<html lang="ja">
	<head>
		<meta charset="UTF-8">
		<title>MUSIC SNS</title>
		<link rel="stylesheet" href="/assets/css/index.css">
		<link href="https://fonts.googleapis.com/css?family=Righteous" rel="stylesheet">
	<body>
		<nav class="center-horizontal">
			<a href="/">MUSIC SNS!!!</a>
			<% if current_user.nil? %>
				<a href="/signup">Sign up</a>
			<% else %>
				<% unless current_user.user_image_url.empty? %>
					<a href="/mypage"><img src="<%= current_user.user_image_url %>" alt="user_image" width="40" height="40"></a>
				<% end %>	
				<a href="/search">Search</a>
				<a href="/signout">Sign out</a>
			<% end %>
		</nav>

		<main>
			<%= yield %>
		</main>
	</body>
</html>
